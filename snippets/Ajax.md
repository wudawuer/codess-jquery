## Ajax
#### jqAjax
Perform an asynchronous HTTP (Ajax) request.
```
$.ajax({
	type: "${method}",
	url: "${url}",
	data: "${data}",
	dataType: "${dataType}",
	success: function (response) {
		$0
	}
});
```