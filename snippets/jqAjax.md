## jqAjax
#### jqAjax
Perform an asynchronous HTTP (Ajax) request.
```javascript
$.ajax({
	type: "${method}",
	url: "${url}",
	data: "${data}",
	dataType: "${dataType}",
	success: function (response) {
		$0
	}
});
```